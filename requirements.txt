# ── 필수 패키지 (반드시 설치) ───────────────────────────────────────────
pandas>=2.0.0                # submission.csv 생성 및 검증 (validator, upstage_tracker)
cryptography>=42.0.0         # AES-256-GCM 복호화 (decryptor.py)
numpy>=1.26.0                # 검색 점수 계산
pymupdf>=1.24.0              # PDF 파싱 (fitz)
rank-bm25>=0.2.2             # BM25 keyword retrieval
scikit-learn>=1.4.0,<1.7.0   # TF-IDF retrieval, 일부 Mac/conda 호환성 고려
certifi>=2024.0.0            # macOS SSL 인증서 체인 안정화
chromadb>=0.5.0              # persistent dense index
sentence-transformers>=3.0.0 # BGE embedding + CrossEncoder reranking

# ── 선택 패키지 (이번 MVP 기본 구현에서는 사용 안 함) ─────────────────────
# pdfplumber>=0.11.0         # 대체 PDF parser
# pypdf>=4.2.0               # 대체 PDF parser
# faiss-cpu>=1.8.0           # Apple Silicon / conda 조합에서 설치 이슈가 잦아 optional 유지

# ── 대형 프레임워크 (비추천) ────────────────────────────────────────────
# langchain>=0.2.0
# llama-index>=0.10.0

# ── LLM API (Upstage 외 다른 API 사용 시) ───────────────────────────────
# openai>=1.0.0
# anthropic>=0.25.0
